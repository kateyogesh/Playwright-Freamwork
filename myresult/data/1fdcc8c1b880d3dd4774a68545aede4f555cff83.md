# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: cookies.spec.ts >> login with saved details
- Location: tests\cookies.spec.ts:43:5

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('Products', { exact: true })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" getByText('Products', { exact: true }) with timeout 5000ms
  - waiting for getByText('Products', { exact: true })

```

```yaml
- text: Swag Labs
- main:
  - form "Login":
    - textbox "Username"
    - textbox "Password"
    - button "Login"
  - heading "Accepted usernames are:" [level=4]
  - text: standard_user locked_out_user problem_user performance_glitch_user error_user visual_user
  - heading "Password for all users:" [level=4]
  - text: secret_sauce
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import fs from 'fs';
  3  | 
  4  | const cookiesData = './storage-data/cookies.data.json';
  5  | const url = 'https://www.saucedemo.com/';
  6  | 
  7  | test.describe.configure({ mode: 'serial' });
  8  | 
  9  | // Test 1: Login and store cookies
  10 | test('store login details', async ({ browser }) => {
  11 | 
  12 |     const context = await browser.newContext();
  13 |     const page = await context.newPage();
  14 | 
  15 |     await page.goto(url);
  16 | 
  17 |     await page.getByRole('textbox', { name: 'Username' })
  18 |         .fill('standard_user');
  19 | 
  20 |     await page.getByLabel('Password')
  21 |         .fill('secret_sauce');
  22 | 
  23 |     await page.locator('#login-button').click();
  24 | 
  25 |     const dashboard = page.getByText('Products', { exact: true });
  26 | 
  27 |     await expect(dashboard).toBeVisible();
  28 | 
  29 |     const cookies = await context.cookies();
  30 | 
  31 |     fs.writeFileSync(
  32 |         cookiesData,
  33 |         JSON.stringify(cookies, null, 2)
  34 |     );
  35 | 
  36 |     console.log('Cookies saved successfully');
  37 | 
  38 |     await context.close();
  39 | });
  40 | 
  41 | 
  42 | // Test 2: Login using saved cookies
  43 | test('login with saved details', async ({ browser }) => {
  44 | 
  45 |     const context = await browser.newContext();
  46 | 
  47 |     const savedCookies = JSON.parse(
  48 |         fs.readFileSync(cookiesData, 'utf8')
  49 |     );
  50 | 
  51 |     await context.addCookies(savedCookies);
  52 | 
  53 |     const page = await context.newPage();
  54 | 
  55 |     await page.goto(url);
  56 | 
  57 |     const dashboard = page.getByText('Products', { exact: true });
  58 | 
> 59 |     await expect(dashboard).toBeVisible();
     |                             ^ Error: expect(locator).toBeVisible() failed
  60 | 
  61 |     console.log('Login successful using saved cookies');
  62 | 
  63 |     await context.close();
  64 | });
```